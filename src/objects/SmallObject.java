package objects;

public interface SmallObject {
    // Igual à interface NotExplodable, não é necessário nenhum função em particular, serve exclusivamente para tornar o projeto mais escalável e para identificar os objetos pequenos
}
