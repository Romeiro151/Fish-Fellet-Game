package objects;

public interface NotExplodable {
    // Esta interface não precisa de métodos por agora, serve simplesmente para tornar o meu projeto mais escalável (caso eu adicione outro Objeto não explodível basta implementar esta interface)
}
